{{! Organization Reference item template — matches VC-20 }}
<div class='org-reference'>

  {{! Header — Red-Stamped }}
  <header class='org-ref-header'>
    <div class='header-left'>
      <h1 class='header-title'>{{localize 'NEONRELIC.OrgRef.Title'}}</h1>
      <div class='header-subtitle'>{{localize 'NEONRELIC.OrgRef.HeaderSubtitle'}}</div>
    </div>
    <div class='header-right'>
      <div class='classification'>{{localize 'NEONRELIC.OrgRef.Classification'}}</div>
      <div class='org-ref-formref'>{{localize 'NEONRELIC.OrgRef.FormRef'}}</div>
      <div class='org-ref-caseid'>
        <span class='field-label'>{{localize 'NEONRELIC.OrgRef.CaseId'}}</span>
        <input
          class='field-value case-id-input'
          type='text'
          name='system.caseId'
          value='{{system.caseId}}'
          {{disabled (not isEditable)}}
        />
      </div>
    </div>
  </header>

  {{! Tab Navigation — at the top, right below the header }}
  <nav class='org-tabs tabs' data-group='org-primary'>
    <a class='item active' data-tab='details' data-group='org-primary'>
      {{localize 'NEONRELIC.OrgRef.TabDetails'}}
    </a>
    <a class='item' data-tab='npcs' data-group='org-primary'>
      {{localize 'NEONRELIC.OrgRef.TabNpcs'}}
    </a>
  </nav>

  {{! Tab Content }}
  <section class='org-tab-content'>
    {{! Details Tab — contains identity block + detail fields }}
    <div class='tab active' data-tab='details' data-group='org-primary'>
      {{! Organization Identity Block }}
      <div class='org-id-block'>
        <div class='org-id-row'>
          <div class='org-id-field'>
            <span class='field-label'>{{localize 'NEONRELIC.OrgRef.OrganizationId'}}</span>
            <input
              class='field-value'
              type='text'
              name='system.organizationId'
              value='{{system.organizationId}}'
              placeholder='O-1'
              {{disabled (not isEditable)}}
            />
          </div>
          <div class='org-desc-field'>
            <span class='field-label'>{{localize 'NEONRELIC.Description'}}</span>
            {{#if isEditable}}
              <prose-mirror
                name='system.description'
                value='{{system.description}}'
                data-document-uuid='{{document.uuid}}'
                relative
              ></prose-mirror>
            {{else}}
              <div class='field-value editor'>{{{enrichedDescription}}}</div>
            {{/if}}
          </div>
        </div>

        {{! Status Checkboxes }}
        <div class='org-status-row'>
          <label class='status-check'>
            <input type='checkbox' name='system.isActive' {{checked system.isActive}} {{disabled (not isEditable)}} />
            <span>{{localize 'NEONRELIC.OrgRef.Active'}}</span>
          </label>
          <label class='status-check'>
            <input type='checkbox' name='system.isDormant' {{checked system.isDormant}} {{disabled (not isEditable)}} />
            <span>{{localize 'NEONRELIC.OrgRef.Dormant'}}</span>
          </label>
        </div>
      </div>
    </div>

    {{! NPCs Tab }}
    <div class='tab' data-tab='npcs' data-group='org-primary'>
      <div class='org-npc-tab'>
        {{#if linkedNpcs.length}}
          <ul class='org-npc-list'>
            {{#each linkedNpcs}}
              <li class='org-npc-item' data-uuid='{{this.uuid}}'>
                <img class='org-npc-img' src='{{this.img}}' alt='{{this.name}}' />
                <span class='org-npc-name'>{{this.name}}</span>
                <button
                  type='button'
                  class='org-npc-remove'
                  data-action='removeNpc'
                  data-uuid='{{this.uuid}}'
                  data-tooltip='{{localize "NEONRELIC.OrgRef.RemoveNpc"}}'
                  {{disabled (not @root.isEditable)}}
                >&times;</button>
              </li>
            {{/each}}
          </ul>
        {{else}}
          <div class='org-npc-empty'>
            <p>{{localize 'NEONRELIC.OrgRef.NoNpcs'}}</p>
          </div>
        {{/if}}
        {{#if isEditable}}
          <div class='org-npc-dropzone'>
            <span>{{localize 'NEONRELIC.OrgRef.DropNpcHint'}}</span>
          </div>
        {{/if}}
      </div>
    </div>
  </section>

  {{! Footer }}
  <footer class='org-ref-footer'>
    <span>VC-20 • The Verdant Covenant • DA Use Only</span>
    <span>Neon Relic</span>
  </footer>
</div>